language: scala
scala:
  - 2.9.2
  - 2.9.1
  - 2.10.2
jdk:
  - openjdk6
  - openjdk7
  - oraclejdk7
env:
  - PLAY_VERSION=2.1.5

before_script:
  - wget http://download.playframework.org/releases/play-${PLAY_VERSION}.zip
  - unzip -q play-${PLAY_VERSION}.zip
script:
  -  sbt test

notifications:
email: false
